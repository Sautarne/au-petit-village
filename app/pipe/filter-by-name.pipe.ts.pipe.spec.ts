import { FilterByNamePipeTsPipe } from './filter-by-name.pipe.ts.pipe';

describe('FilterByNamePipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNamePipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});
